# Current product walkthrough

[Watch on Drive](https://drive.google.com/file/d/19CIu9tWAfKNo1nFfHzACgcnQIv-oUi_C/view). The existing file ID is retained so the submitted link stays valid.

Recorded September 9, 2026, from real browser interactions with the deployed app at 1280 × 960. The final captioned export is 81.48 seconds, H.264, 1920 × 1440, 60 fps, silent. The output is upscaled from the browser capture; the source recording is not native 60 fps.

The walkthrough shows the landing, verified account, two-step brief, a saved test event, Firecrawl source lead, enquiry review, upload controls, computed totals, changing guest count, revision history and original document links. It uses previously processed fictional quotes. It does not claim to send a fresh enquiry, run discovery or perform a new extraction during recording.

The submitted Drive video replaces the earlier 86-second version. The September 8 X post and GitHub release preserve the earlier footage. Binary video files remain outside Git history.
