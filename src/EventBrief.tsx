import {useState,useRef,type FormEvent} from "react";
import {useMutation} from "convex/react";
import {ConvexError} from "convex/values";
import {api} from "../convex/_generated/api";
import type {Id} from "../convex/_generated/dataModel";
export default function EventBrief({onCreated,onCancel,discovery}:{onCreated:(id:Id<"events">)=>void;onCancel:()=>void;discovery:boolean}){
 const create=useMutation(api.events.create),form=useRef<HTMLFormElement>(null);
 const [step,setStep]=useState(1),[busy,setBusy]=useState(false),[error,setError]=useState("");
 async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(step===1){setStep(2);return;}const d=new FormData(e.currentTarget);setBusy(true);setError("");try{onCreated(await create({title:String(d.get("title")),city:String(d.get("city")),eventDate:String(d.get("date")),headcount:Number(d.get("count")),dietary:String(d.get("dietary")) as "veg"|"nonveg"|"both",eventType:String(d.get("eventType")),neighbourhood:String(d.get("neighbourhood")||"").trim(),dateFlexible:d.get("flexible")==="on",...(d.get("budget")?{budgetHint:Number(d.get("budget"))}:{}),needs:d.getAll("needs").map(String),autoDiscover:d.get("search")==="on"}));}catch(e){setError(e instanceof ConvexError && typeof e.data==="string"?e.data:"Could not create your event. Check your connection and try again.");}finally{setBusy(false);}}
 return <div className="brief-layout"><aside className="brief-intro"><h1>A place for<br/>your people.</h1><p>Start with the gathering. We’ll help you put the venue options in order.</p><ol className="brief-steps"><li aria-current={step===1?"step":undefined}>The gathering</li><li aria-current={step===2?"step":undefined}>What matters to you</li></ol><button className="quiet" onClick={onCancel}>Back to my events</button></aside><form ref={form} className="brief-form" onSubmit={submit}><div className="form-step" hidden={step!==1}><p className="step-count">Step 1 of 2</p><h2>Tell us about the day.</h2><div className="fields"><label className="full-field">Event name<input name="title" required maxLength={100} placeholder="e.g. Mum’s 60th birthday"/></label><label>Occasion<select name="eventType"><option>Family reception</option><option>Wedding</option><option>Birthday</option><option>Anniversary</option><option>Corporate gathering</option><option>Other gathering</option></select></label><label>Guests<input name="count" required type="number" min={1} max={2000} defaultValue={120}/></label><label>City<input name="city" required maxLength={80} placeholder="Mumbai"/></label><label>Neighbourhood <span className="optional">Optional</span><input name="neighbourhood" maxLength={80} placeholder="Bandra, Andheri…"/></label><label className="full-field">Preferred date<input name="date" required type="date"/></label></div><label className="followup-choice"><input type="checkbox" name="flexible"/>I can consider nearby dates</label></div><div className="form-step" hidden={step!==2}><p className="step-count">Step 2 of 2</p><h2>Make it your kind of gathering.</h2><div className="fields"><label>Menu<select name="dietary"><option value="veg">Vegetarian</option><option value="nonveg">Non-vegetarian</option><option value="both">Both menus</option></select></label><label>Total budget (INR) <span className="optional">Optional</span><input name="budget" type="number" min={1} max={100000000} placeholder="200000"/></label></div><p className="note">Your venue + food budget stays private. We use it to compare quotes; venues won’t see it.</p><fieldset className="need-options"><legend>Must-haves</legend>{["Parking","Wheelchair access","Outdoor space","Air conditioning","Stage","Rooms for guests"].map(n=><label key={n}><input name="needs" type="checkbox" value={n}/>{n}</label>)}</fieldset><label className="followup-choice"><input type="checkbox" name="search" defaultChecked={discovery} disabled={!discovery}/>Search for venue contacts when I finish</label><p className="note">You choose the shortlist and approve every enquiry before it is sent.</p></div><div className="form-actions">{step===2 && <button type="button" className="quiet" onClick={()=>setStep(1)} disabled={busy}>Back</button>}<button type={step===1?"button":"submit"} disabled={busy} onClick={step===1?(e)=>{e.preventDefault();if(form.current?.reportValidity())setStep(2);}:undefined}>{busy?"Creating your event…":step===1?"Continue":"Create my event"}</button></div>{error && <p className="error" role="alert">{error}</p>}</form></div>;
}
